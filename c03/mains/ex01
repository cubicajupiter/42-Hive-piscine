int	ft_strncmp(char *s1, char *s2, unsigned int n);

int	main(void)
{
	#include <stdio.h>
	#include <string.h>
	char	s1[] = "Hedgdf";
	char	s2[] = "Hello worle";
	int	diff;
	diff = ft_strncmp(s1, s2, 4);
	int	ref = strncmp(s1, s2, 4);
	printf("%d\n", diff);
	printf("%d\n", ref);
	return (0);
}
